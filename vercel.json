{
  "version": 2,
  "buildCommand": "npm run build --workspace=web-app",
  "outputDirectory": "web-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
