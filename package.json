{
  "name": "grocery-deals-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web-app",
    "backend",
    "admin"
  ],
  "scripts": {
    "build": "npm run build --workspace=web-app",
    "vercel-build": "npm run build --workspace=web-app"
  }
}
