{
  "name": "grocery-deals-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-app && npm install && npm run build",
    "vercel-build": "cd web-app && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "typescript": "^5.5.2"
  }
}
